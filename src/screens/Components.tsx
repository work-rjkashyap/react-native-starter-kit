import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

type Props = {};

const ComponentsScreen = (props: Props) => {
  return (
    <View>
      <Text>Components</Text>
    </View>
  );
};

export default ComponentsScreen;

const styles = StyleSheet.create({});
